# Bot framework
aiogram>=3.20.0
aiohttp            # transitive dep of aiogram — old version has no cp314 wheel
aiofiles==24.1.0

# Database & cache
asyncpg          # 0.30.0 has no cp314 wheel — let pip pick latest
redis==5.2.1

# AI
openai==1.75.0

# Config
pydantic>=2.10.0,<2.12.0  # pydantic-core >=2.27 ships cp314 wheels; avoid 2.12+ until cp314 confirmed
pydantic-settings==2.9.0
python-dotenv==1.1.0

# Google Cloud TTS & APIs
google-cloud-texttospeech==2.26.0
google-api-python-client==2.167.0

# Audio
pydub==0.25.1
soundfile        # 0.13.1 needs cffi/libsndfile build — let pip pick latest

# NLP middleware (classes.py)
spacy==3.8.11
en_core_web_sm @ https://github.com/explosion/spacy-models/releases/download/en_core_web_sm-3.8.0/en_core_web_sm-3.8.0-py3-none-any.whl
nltk==3.9.1
language-tool-python==2.9.3
pyspellchecker==0.8.2

# Misc utilities
Pillow           # 11.2.1 has no cp314 wheel — let pip pick latest
gender-guesser==0.4.0
requests==2.32.3

# Tests
pytest==8.3.5
pytest-asyncio==0.24.0
